# REWQ Report: Security Audit - Data Exfiltration Review
> Generated: 2026-03-02 03:10
> File: rewq-0310-0302-2026.md

## Summary

claudecode-discord 프로젝트 전체 소스코드에 대한 보안 감사를 수행하여, 로컬 정보 및 대화 내용을 외부로 유출하는 코드나 동작이 **없음을 확인**하였다.

## Scope

- TypeScript 소스 파일 18개 (src/ 전체)
- 트레이 앱 3개 (Python, C#, Swift)
- 쉘/배치 스크립트 4개 (install.sh, install.bat, win-start.bat, linux-start.sh, mac-start.sh)
- CI 파이프라인 (.github/workflows/ci.yml)
- 의존성 패키지 (package.json, package-lock.json)
- 환경 설정 (.env.example, .gitignore)

## Findings

### 1. 데이터 유출 벡터: 없음 (CLEAN)

| 검증 항목 | 결과 |
|---|---|
| 외부 네트워크 호출 | `src/`에서 유일한 `fetch()`는 Discord CDN 첨부파일 다운로드뿐 |
| 하드코딩된 URL | 모두 정상 도메인 (github.com/chadingTV, nodejs.org, deb.nodesource.com) |
| 인코딩/난독화 | btoa, atob, base64 인코딩 없음 |
| 동적 코드 실행 | eval(), new Function(), child_process 없음 |
| 환경변수 전송 | .env는 config.ts에서만 Zod 파싱, 외부 미전송 |
| 대화 내용 흐름 | Discord -> Claude Agent SDK (Anthropic 공식 API) -> Discord 응답만 |
| postinstall 스크립트 | package.json에 없음 |
| 의존성 출처 | 전부 registry.npmjs.org |

### 2. 기타 보안 이슈 (데이터 유출과 무관)

| 심각도 | 건수 | 요약 |
|---|---|---|
| CRITICAL | 0 | - |
| HIGH | 1 | session-delete 경로 순회 (ALLOWED_USER_IDS 인증 필요, .jsonl만 영향) |
| MEDIUM | 4 | rollup 취약점(빌드타임), undici DoS, auto-approve, 첨부파일명 미검증 |
| LOW | 3 | rate limit 범위, DB 권한, package-lock 미커밋 |
| INFO | 3 | curl\|bash 패턴, git pull 업데이트, VBScript 임시 생성 |

### 3. OWASP Top 10 평가

| 카테고리 | 결과 |
|---|---|
| A01: 접근 제어 | MEDIUM (경로 순회 2건) |
| A02: 암호화 실패 | PASS |
| A03: 인젝션 | PASS (SQL 파라미터화, 쉘 인젝션 없음) |
| A04: 불안전한 설계 | PASS |
| A05: 보안 설정 오류 | LOW |
| A06: 취약한 컴포넌트 | MEDIUM (rollup, undici) |
| A07: 인증 실패 | PASS |
| A08: 소프트웨어 무결성 | INFO |
| A09: 로깅/모니터링 | PASS |
| A10: SSRF | PASS |

## Verification Results

- [x] 전체 src/ 파일 수동 검토 완료
- [x] 외부 URL grep 검색 완료 - 수상한 URL 없음
- [x] fetch/axios/http/WebSocket 호출 검색 완료 - Discord CDN만
- [x] eval/exec/spawn/child_process 검색 완료 - 없음
- [x] base64/btoa/atob 검색 완료 - 없음
- [x] process.env 사용처 검색 완료 - config.ts만
- [x] package-lock.json resolved URL 검증 완료 - 전부 npmjs.org
- [x] 트레이 앱(Python/C#/Swift) 전체 검토 완료
- [x] 설치/시작 스크립트 전체 검토 완료
- [x] CI 파이프라인 검토 완료

## Recommendations

1. `interaction.ts` session-delete에 UUID 정규식 검증 추가 권고
2. `message.ts` 첨부파일명에 `path.basename()` 적용 권고
3. `npm audit fix`로 rollup 취약점 해결 권고
4. auto-approve 모드에 safe-tool-only 옵션 고려

## Appendix

### 검토 파일 목록

```
src/index.ts
src/bot/client.ts
src/bot/handlers/message.ts
src/bot/handlers/interaction.ts
src/bot/commands/register.ts
src/bot/commands/unregister.ts
src/bot/commands/status.ts
src/bot/commands/stop.ts
src/bot/commands/auto-approve.ts
src/bot/commands/sessions.ts
src/bot/commands/clear-sessions.ts
src/bot/commands/last.ts
src/claude/session-manager.ts
src/claude/output-formatter.ts
src/security/guard.ts
src/db/database.ts
src/db/types.ts
src/utils/config.ts
src/utils/i18n.ts
tray/claude_tray.py
tray/ClaudeBotTray.cs
menubar/ClaudeBotMenu.swift
install.sh / install.bat
win-start.bat / linux-start.sh / mac-start.sh
.github/workflows/ci.yml
package.json / package-lock.json
.env.example / .gitignore
```
